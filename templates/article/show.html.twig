{% extends "base.html.twig" %}

{% block title %}Show Article{% endblock %}

{% block body %}

    <a href="{{ path('liste_article')}}" class="btn btn-danger mt-3">Retour à la liste</a>

    <div class="container-fluid p-5">
    
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header"><h3 class="text-center">{{article.titre}}</h3></div>
                    <div class="card-body text-center">
                        {{article.contenu}}
                    </div>
                    <div class="card-footer text-center">
                        <p>paru le : {{article.createdAt | date('d-m-Y')}}</p>
                        <p>Ecrit par : {{article.auteur.prenom}} {{article.auteur.nomDeFamille}} </p>
                    </div>

                    <a href="{{ path('supprimer_article', {'id':article.id})}}" class="btn btn-danger mt-3 ">Supprimer l'Article</a>
                </div>
            </div>

            
        </div>
    
    
    </div>





{% endblock %}